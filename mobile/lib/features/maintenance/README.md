# maintenance

Phase 0 scaffolding placeholder. Blueprint §4.6.

This feature will hold the maintenance module:

- `models/` — maintenance schedule and work-order records.
- `providers/` — schedule list, record-work flow, reminders.
- `screens/` — maintenance schedule, record-work form.
- `widgets/` — schedule tiles, service-type chips, due badges.

Screens/providers arrive in Phase 1+; only the directory skeleton exists today.
