# fleet

Phase 0 scaffolding placeholder. Blueprint §4.1.

This feature will hold the truck fleet management module:

- `models/` — truck, health, and maintenance records.
- `providers/` — fleet CRUD, health status, per-truck state.
- `screens/` — fleet list, truck detail, health dashboard.
- `widgets/` — fleet tiles, health indicators, status chips.

Screens/providers arrive in Phase 1+; only the directory skeleton exists today.
