# clients

Phase 0 scaffolding placeholder. Blueprint §4.3.

This feature will hold the CRM module:

- `models/` — client and contact models.
- `providers/` — client list, detail, contact management.
- `screens/` — client list, client detail, merge flow.
- `widgets/` — client cards, contact rows, merge pickers.

Screens/providers arrive in Phase 1+; only the directory skeleton exists today.
