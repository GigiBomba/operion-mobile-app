org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template (classic-KGP
# plugins such as connectivity_plus require it disabled under AGP 9).
# NOTE: file_picker 11.x skips the classic kotlin-android plugin on AGP 9+;
# the root build.gradle.kts force-applies KGP to it (see there).
android.builtInKotlin=false
