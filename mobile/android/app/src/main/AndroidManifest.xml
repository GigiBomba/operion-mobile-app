<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- GPS & Location (blueprint §7.2.3: turn-by-turn navigation is
         phone-GPS-driven. Minimum permission set required by geolocator for
         foreground + background tracking. ACCESS_BACKGROUND_LOCATION is
         requested after the foreground grant (geolocator two-step flow);
         FOREGROUND_SERVICE_LOCATION + POST_NOTIFICATIONS (Android 13+) are
         required for the persistent foreground-service notification.) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Blueprint §12: biometric confirmation for sensitive finance actions
         (invoice finalize/submit/mark-paid + CMR signature). local_auth merges
         these itself; declaring them explicitly is belt-and-braces for
         devices/emulators where the merged manifest is stripped. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <!-- Phase 5A rich notifications (§9.3): boot rescheduling of the bounded
         Snooze re-show alarm. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="operion_mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Phase 5A home-screen widgets (§9.1): the home_widget plugin
                 uses this action to launch the app when a widget is tapped. -->
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH" />
            </intent-filter>
        </activity>

        <!-- Phase 5A: "Today's KPIs" AppWidget (dispatcher/manager) — the
             provider is bridged to the home_widget plugin, which pushes the
             data written via HomeWidget.saveWidgetData/updateWidget. -->
        <receiver
            android:name=".OperionWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/operion_widget_info" />
        </receiver>

        <!-- Phase 5A: "Next stop / active trip" AppWidget (driver role). -->
        <receiver
            android:name=".OperionDriverWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/operion_driver_widget_info" />
        </receiver>

        <!-- Phase 5A: home_widget interactive / background-callback receiver. -->
        <receiver
            android:name="es.antonborri.home_widget.HomeWidgetBackgroundReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.BACKGROUND" />
            </intent-filter>
        </receiver>

        <!-- Phase 5A rich notifications (§9.3): scheduled-notification
             receivers required by flutter_local_notifications (Snooze
             re-show + rescheduling after reboot). -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Foreground service for turn-by-turn navigation (§7.2.3).
             Declared per the flutter_foreground_task README: the service name
             is plugin-required and must NOT be changed. The "location"
             foregroundServiceType matches the runtime serviceTypes in
             ForegroundNavigationController (Android 14+ requirement) and the
             service keeps the process alive so backgrounded navigation — next
             instruction + remaining distance in the persistent notification —
             keeps updating while the app is backgrounded/screen-locked.
             stopWithTask=true: swiping the app away ends navigation with the
             screen. Live visibility of the notification is DEVICE-REQUIRED
             (blueprint §7.2.4(c), verified in QA drive-test). -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"
            android:stopWithTask="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
